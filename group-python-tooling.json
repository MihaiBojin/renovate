{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Python packaging and dev tooling (build, pip, pre-commit, pytest, twine, wheel, wheel-inspect, toml) never touches runtime behavior; one PR per tool multiplies review overhead for the updates with the smallest blast radius in the repo.",
  "packageRules": [
    {
      "description": "One PR for non-major Python packaging/dev tooling updates",
      "groupName": "python dev tooling",
      "matchPackageNames": [
        "build",
        "pip",
        "pre-commit",
        "pytest",
        "toml",
        "twine",
        "wheel",
        "wheel-inspect"
      ],
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"]
    }
  ]
}
